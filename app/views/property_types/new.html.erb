
<h1>Cadastro de Tipos de Imóveis</h1>
<% if @property_type.errors.any? %>
   <ul>
      <% @property_type.errors.full_messages.each do |error| %>
         <li><%= error %></li>
      <% end %>
   </ul>
<% end %>
<%= form_with model: @property_type do |p|%>
  <%= p.label :name, "Nome" %>
  <%= p.text_field :name %>
  <%= p.submit "Enviar"%>
  
<% end %>
